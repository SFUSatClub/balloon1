#include <Arduino.h>
// typedef struct{
//   uint16_t Interval;
//   uint32_t LastTick;
//   void (*Func)(void);
// }TaskType;

class schedule{
private:



public:
  schedule(); // constructor - we set up the timer here... this should probably go somewhere else lol


  //
  // TaskType *getConfig(void);
  // uint8_t task_getNumTasks(void);

}
